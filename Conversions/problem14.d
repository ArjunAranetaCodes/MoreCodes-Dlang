//Problem 14: Write a program that converts lower case to upper case characters.
import std.stdio;
import std.string;
import std.conv;
import std.ascii;

int main(){
 string str = toUpper("abc");
 
 writeln(str);
  
 return 0;
}