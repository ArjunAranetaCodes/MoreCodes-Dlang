//Problem 15: Write a program that converts letters to ASCII.
import std.stdio;
import std.string;
import std.conv;
import std.ascii;

int main(){
 char letter = 'x';
 
 writeln(to!int(letter));
  
 return 0;
}